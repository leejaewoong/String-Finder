### L10N String ID 자동화 PoC (2026-08-31)

#### 신규 기능

- Figma `스트링 태그`의 `%stringTag` 노드 정보를 읽어 국문과 내보낼 최상위 프레임을 찾습니다.
- 위키에 표가 없으면 프레임 이미지와 `구분자 | 이미지 | 국문 | 영문 | String ID` 표를 생성합니다.
- 위키 표가 있으면 Figma와 비교한 정상 행에만 String ID 후보를 작성하며, 태그·행·국문 불일치는 확인 필요 항목으로 남깁니다.
- 동일한 `Text + Type`의 기존 COMMON 키를 우선하고, 여러 피처에 걸친 동일 문구는 COMMON을 추천합니다.
- 기존 키 재사용은 JSON을 변경하지 않으며, 신규 항목만 최종 확정 시 로컬 GDD에 반영합니다.
- JSON 반영 전 백업과 실패 시 롤백을 지원합니다.
- 위키/Figma 제목의 `vNNMM`에서 PC Release Date를 자동 제안합니다.
- 진행 상태 배지, 반복 `String ID 생성`, 최종 확정, 작업 취소를 포함한 전용 탭을 추가했습니다.

#### PoC 범위

- 토큰은 개발 환경의 `.env` 또는 설치 앱의 `%APPDATA%\String-Finder\.env`에서 읽습니다.
- Git pull, commit, push와 사내 Gateway 연동은 포함하지 않습니다.

---

### v1.1.2 (2026-04-13)

#### 🐛 영문 검색 시 UNRELEASED 태그 오표기 버그 수정
- 영문 검색 시 선택한 언어와 상관없이 INPUT 폴더의 Value값 반환 버그 수정 (Spotted by. 👩🏻주용님)

---

### v1.1.1 (2026-04-10)

#### 🗝️ API 키 유효기간 만료로 교체
- 3개월간의 API 키 유효기간이 만료되어 교체했습니다. 

#### 개선사항
- 🔖 L10N되지 않은 STRING ID의 경우 UNRELEASED 태그가 함께 표시됩니다.

---

### v1.1.0 (2026-01-16)

#### 🧠 공식 지원 API 적용
- 드디어 OGU에서도 Open AI API가 지원됩니다 🎉

#### 개선사항
- 🤖 AI 번역 기능에 더 나은 모델을 채택했습니다. (gpt-4o-mini >> gpt-4.1-mini)
- 🎨 앱 타이틀바 아이콘 깨짐 버그 수정
- 👦🏻 서비스명을 변경했습니다(제 취향입니다;;;)  
- 🙏 설정 변경으로 이전 버전에서의 검색 기록이 사라집니다. (이후 업데이트에서는 유지됩니다.)

---

### v1.0.0 (2025-11-08)

#### 🎉 첫 릴리스
- 앞으로 잘 부탁드립니다 🙏

#### 개선사항
- 📚 INPUT 폴더 탐색 범위 포함 (릴리즈 되지 않은 String 검색 가능)
- 🎨 검색 옵션 UI 변경
- 🔎 등록된 String이 없는 경우 'AI번역', '유의어 검색' 기능 안내
- 🗒️ 패치 노트 기능 추가
- ❓ 사용 가이드 기능 추가

#### 버그 수정
- 🐛 Git Pull 완료 시 Alert으로 인한 포커스 불가 현상 수정
- 🔧 검색어 앞뒤 공백 제거 로직 추가

---

### v0.9.0 (2025-10-28)

#### 베타 테스트 
- 의견 주신 분들: 👩🏻‍🦰미진님, 👦🏻경진님, 👩🏻주용님, 👩🏻선미님, 👧🏻슬기님
